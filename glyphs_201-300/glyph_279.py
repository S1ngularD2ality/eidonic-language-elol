def glyph_279(binary_list):
    """
    Glyph 279 — Binary Pulse Counter
    Counts number of 1s in a binary list.

    Example Input:
    [0, 1, 1, 0, 1]

    Output:
    3
    """
    return sum(binary_list)
