def glyph_238(arr):
    """
    Glyph 238 — Quantum Leap Remapper
    Maps input numbers to new values using quadratic leap logic.

    Example Input:
    [1, 2, 3]

    Output:
    [1, 4, 9]
    """
    return [x**2 for x in arr]
