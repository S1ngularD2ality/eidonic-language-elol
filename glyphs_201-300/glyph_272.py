def glyph_272(val, count):
    """
    Glyph 272 — Vector Phase Repeater
    Creates a list repeating value N times.

    Example Input:
    (7, 3)

    Output:
    [7, 7, 7]
    """
    return [val] * count
