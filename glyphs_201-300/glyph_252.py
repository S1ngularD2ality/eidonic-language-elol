def glyph_252(arr):
    """
    Glyph 252 — Sequential Peak Locator
    Finds the index of the highest value in the array.

    Example Input:
    [1, 3, 7, 5, 2]

    Output:
    2
    """
    return arr.index(max(arr))
