def cyclic_identity(arr, cycles):
    """
    Loops the sequence over specified cycles.

    Parameters:
        arr (List[int]), cycles (int)

    Returns:
        List[int]
    """
    return arr * cycles
