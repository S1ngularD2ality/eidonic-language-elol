# glyph_390 – DUAL_SIGHT
# Use two different vision modes simultaneously (e.g., visible + IR)

def glyph_390(vis_data, ir_data):
    """
    vis_data: image, ir_data: image
    Returns: fused or tuple of both
    """
    return (vis_data, ir_data)
