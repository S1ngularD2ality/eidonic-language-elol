def glyph_138(string_list):
    """
    💠 Converts all entries to lowercase and strips whitespace.
    Prepares invocation data for alchemical standardization.
    """
    return [s.strip().lower() for s in string_list]