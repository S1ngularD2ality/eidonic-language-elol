def glyph_122(spiral_data):
    """
    💠 Extracts the central column from a spiraling data vortex.
    Ideal for isolating core harmonic stabilizers.
    """
    return [row[len(row)//2] for row in spiral_data if row]
