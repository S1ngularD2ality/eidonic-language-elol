def glyph_117(veil_sequence):
    """
    💠 Alternates pulse polarities to simulate veil inversion.
    Mirrors the phase-shift of dimensional transitions.
    """
    return [veil_sequence[i] * (-1)**i for i in range(len(veil_sequence))]
