# glyph_651 – MULTI_BOT_FORMATION
# Arrange multiple bots into safe cooperative formation

def glyph_651(bot_positions, formation_type='line'):
    return f"Formation {formation_type} set for {len(bot_positions)} bots"
