def replicate_patch(patch, times):
    """
    Replicates a symbolic patch multiple times.

    Parameters:
        patch (List[int]): Core patch.
        times (int): Number of replications.

    Returns:
        List[int]: Expanded fractal chain.
    """
    return patch * times
