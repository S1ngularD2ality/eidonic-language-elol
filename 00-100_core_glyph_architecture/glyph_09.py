def temporal_reverse(sequence):
    """
    Reverses symbolic flow in time by flipping the input list.

    Parameters:
        sequence (List[int]): The symbolic sequence.

    Returns:
        List[int]: Reversed symbolic sequence.
    """
    return sequence[::-1]
