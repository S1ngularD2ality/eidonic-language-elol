def glyph_119(fractal_points):
    """
    💠 Removes redundant signal points to refine fractal geometry.
    Ensures non-dual echoes are filtered.
    """
    return list(set(fractal_points))
