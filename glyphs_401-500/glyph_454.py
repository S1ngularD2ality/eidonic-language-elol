# glyph_454 – BRIDGE_SIGNAL
# Create a signal bridge between incompatible systems

def glyph_454(data, system_a, system_b):
    """
    data: any
    system_a: string
    system_b: string
    Returns: bridge message
    """
    return f"Bridge from {system_a} to {system_b}: {data}"
