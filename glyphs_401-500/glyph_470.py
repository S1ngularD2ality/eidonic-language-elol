# glyph_470 – STATUS_BROADCAST
# Broadcast current status from all agents to the collective

def glyph_470(agent_statuses):
    """
    agent_statuses: dict of agent:status
    Returns: combined dict
    """
    return agent_statuses.copy()
