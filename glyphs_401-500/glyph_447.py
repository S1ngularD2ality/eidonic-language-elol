# glyph_447 – RAPID_HANDOFF
# Instantly pass a task from one agent to another without delay

def glyph_447(task, from_agent, to_agent):
    """
    task: any
    from_agent: string
    to_agent: string
    Returns: handoff confirmation
    """
    return f"Task '{task}' handed from {from_agent} to {to_agent}"
