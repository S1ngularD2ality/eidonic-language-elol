# glyph_398 – PATH_RANDOMIZE
# Randomize path or behavior for evasion, unpredictability, or learning

import random
def glyph_398(paths):
    """
    paths: list of possible routes
    Returns: randomly chosen path
    """
    return random.choice(paths)
