# glyph_440 – OVERWATCH
# Assign a guardian agent to oversee the collective and intervene as needed

def glyph_440(agent_list):
    """
    agent_list: list of agents
    Returns: assigned overwatch agent
    """
    if not agent_list:
        return None
    return agent_list[0]  # Could rotate as needed
