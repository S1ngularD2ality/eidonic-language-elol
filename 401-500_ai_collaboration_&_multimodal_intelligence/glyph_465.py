# glyph_465 – TASK_MERGE
# Merge related tasks into a single coordinated objective

def glyph_465(tasks):
    """
    tasks: list of strings
    Returns: merged string
    """
    return " & ".join(tasks)
